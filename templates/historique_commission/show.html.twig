{% extends 'base.html.twig' %}

{% block title %}HistoriqueCommission{% endblock %}

{% block body %}
    <h1>HistoriqueCommission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historique_commission.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ historique_commission.nom }}</td>
            </tr>
            <tr>
                <th>GainCommission</th>
                <td>{{ historique_commission.gainCommission }}</td>
            </tr>
            <tr>
                <th>TypeCarte</th>
                <td>{{ historique_commission.typeCarte }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ historique_commission.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_historique_commission_index') }}">back to list</a>

    <a href="{{ path('app_historique_commission_edit', {'id': historique_commission.id}) }}">edit</a>

    {{ include('historique_commission/_delete_form.html.twig') }}
{% endblock %}

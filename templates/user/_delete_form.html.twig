<div class="row gy-5">
    <div class="col-12 col-md-6">
        <h4 class="text-black">Supprimer le compte </h4>
        <p class="text-700">Transfer this account to another person or to a company repository.</p>
        <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                <button class="btn btn-phoenix-danger">Supprimer le compte</button>
        </form>
    </div>
</div>
<form method="post" action="{{ path('app_b_m_o_delete', {'id': b_m_o.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_m_o.id) }}">
    <button class="btn btn-danger   "> <span data-feather="trash-2"></span>  Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Modifier un enregistrer BMO{% endblock %}

{% block body %}
      {% include '__fragments/nav.html.twig' %}
      {# contenu #}
      <div class="content">
        <div class="pb-5">
          <div class="row g-4">
            <div class="col-12 col-xxl-6">
                <div class="mb-8">                    
                    <h1>Modifier</h1>

                    {{ include('bmo/_form.html.twig', {'button_label': 'Modiier'}) }}

                    <div class="mt-2 mb-2"> {{ include('bmo/_delete_form.html.twig') }} </div> 

                    <a href="{{ path('app_b_m_o_index') }}">Retour</a>
                </div> 
          </div>
        </div>
        </div>
      </div>
{% endblock %}
